// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("lzo", "for lzo support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("lzo", "enable lzo support", "no");

if (PHP_LZO != "no") {
	EXTENSION("lzo", "lzo.c");
}

